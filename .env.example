# Copy to .env for local overrides (optional — dev.py has safe fallbacks).
# Required in production; see config/settings/prod.py.

DJANGO_SECRET_KEY=change-me-to-a-random-50-char-string
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
